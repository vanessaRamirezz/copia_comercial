// Función para generar datos aleatorios y llenar el formulario de referencias familiares
function llenarFormularioReferenciasFamiliares() {
    const nombres = ["Juan Pérez", "Carlos López", "María Rodríguez", "Luis García"];
    const direcciones = ["Av. Libertad 234, San Salvador", "Calle 15, Santa Ana", "Calle El Sol, Usulután", "Boulevard de la Reforma"];
    const lugaresTrabajo = ["Comercial de Servicios S.A.", "Servicios Financieros S.A.", "Corporación X", "Consultora ABC"];
    const telefono = `${Math.floor(Math.random() * 9000 + 1000)}-${Math.floor(Math.random() * 9000 + 1000)}`; // Generar teléfono sin prefijo
    const cargos = ["Recepcionista", "Auxiliar Administrativo", "Contador", "Asistente de Gerencia"];

    // Generación aleatoria de datos para la primera referencia
    const nombreUno = nombres[Math.floor(Math.random() * nombres.length)];
    const direccionUno = direcciones[Math.floor(Math.random() * direcciones.length)];
    const lugarTrabajoUno = lugaresTrabajo[Math.floor(Math.random() * lugaresTrabajo.length)];
    const telefonoUno = telefono;
    const telefonoTrabajoUno = `${Math.floor(Math.random() * 9000 + 1000)}-${Math.floor(Math.random() * 9000 + 1000)}`;
    
    // Generación aleatoria de datos para la segunda referencia
    const nombreDos = nombres[Math.floor(Math.random() * nombres.length)];
    const direccionDos = direcciones[Math.floor(Math.random() * direcciones.length)];
    const lugarTrabajoDos = lugaresTrabajo[Math.floor(Math.random() * lugaresTrabajo.length)];
    const telefonoDos = telefono;
    const telefonoTrabajoDos = `${Math.floor(Math.random() * 9000 + 1000)}-${Math.floor(Math.random() * 9000 + 1000)}`;

    // Llenar los campos del formulario
    document.getElementById('nombreRefFamiliarUno').value = nombreUno;
    document.getElementById('dirRefFamiliarUno').value = direccionUno;
    document.getElementById('telProRefFamiliarUno').value = telefonoUno;
    document.getElementById('lugarTrabajoRefFamiliarUno').value = lugarTrabajoUno;
    document.getElementById('telTrabaRefFamiliarUno').value = telefonoTrabajoUno;
    
    document.getElementById('nombreRefFamiliarDos').value = nombreDos;
    document.getElementById('dirRefFamiliarDos').value = direccionDos;
    document.getElementById('telProRefFamiliarDos').value = telefonoDos;
    document.getElementById('lugarTrabajoRefFamiliarDos').value = lugarTrabajoDos;
    document.getElementById('telTrabaRefFamiliarDos').value = telefonoTrabajoDos;
}

// Llamar a la función para llenar los datos cuando sea necesario
llenarFormularioReferenciasFamiliares();
